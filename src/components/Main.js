import styled from "styled-components";


const Main = (props) => {
    return (
        <Container>right side</Container>
    )
}

const Container = styled.div`
    grid-area: Main;
`;

export default Main;